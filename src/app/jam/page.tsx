import JamPage from '@/components/jam/JamPage';

export const metadata = {
  title: 'JAM Sessions | Mushflow',
  description: 'Study together with synchronized music and chat',
};

export default function JamRoute() {
  return <JamPage />;
} 